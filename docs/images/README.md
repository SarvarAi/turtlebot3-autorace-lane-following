# Images

Screenshots and diagrams referenced from the documentation.

Worth adding here — each one raises how the repository reads at a glance:

| File | What to capture |
|---|---|
| `decision_view.png` | The `DECISION` debug window mid-run, showing detected lines, target, and telemetry panel |
| `masks.png` | Yellow / white / red masks side by side against the source frame |
| `turn_safety.png` | The `TURN_LEFT` safety corridor overlay with a yellow line encroaching |
| `ros_graph.png` | `rqt_graph` output of the running system |
| `track.jpg` | The physical track |

Both run videos are good sources for stills:
[robot run](https://youtu.be/nZOmZpWM95w) · [screen capture](https://youtu.be/SDK73mEr38g)

Once added, embed them in the README:

```markdown
![Decision view](docs/images/decision_view.png)
```
